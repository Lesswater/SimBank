public class Transfer extends Transactions{
   public void completeTransaction(String[]currentTransaction){
      if(checkIfAccountExists(currentTransaction[1])){
         if(checkIfAccountExists(currentTransaction[2])){
      
      
         }
      }
   }
}